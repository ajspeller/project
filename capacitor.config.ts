import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.certifyyaself.starter',
  appName: 'project',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
